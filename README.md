A dynamic web application built with HTML, CSS, JavaScript, and Supabase. Key Features:

Smart Randomizer: specific algorithm to suggest places while filtering out recently visited locations (Cooldown logic).

Authentication: Secure Sign-up/Login system using Supabase Auth.

Database Management: Users can view personal history and contribute new locations (CRUD operations).

Responsive UI: Modern, user-friendly interface with interactive elements.
